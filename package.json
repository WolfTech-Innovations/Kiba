{
  "name": "kibaos-automation",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "npx prettier --write .",
    "test": "python3 check_workflows.py"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "jsonlint": "^1.6.3",
    "prettier": "^3.8.3"
  }
}

name: Audit Build Su C Eatmydata

on:
  push:
    paths:
      - 'scripts/kibaos_build.sh'
  pull_request:
    paths:
      - 'scripts/kibaos_build.sh'

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Check su -c eatmydata
        run: |
          grep "su -c" scripts/kibaos_build.sh | grep -v "eatmydata" && (echo "eatmydata should be used inside su -c strings" && exit 1) || exit 0

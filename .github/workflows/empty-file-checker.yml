name: Empty File Checker
on:
  push:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Find empty files
        run: |
          find . -type f -empty -not -path "./.git/*" -not -name ".gitkeep" | grep . && { echo "Error: Empty files found"; exit 1; } || echo "No empty files"

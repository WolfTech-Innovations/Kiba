name: Spell Check

on:
  push:
  pull_request:

jobs:
  codespell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run codespell
        uses: codespell-project/actions-codespell@v2
        with:
          ignore_words_file: .codespellignore
          ignore_words_list: vEw
          skip: .git,*.png,*.jpg,*.iso
          check_filenames: true
          check_hidden: true

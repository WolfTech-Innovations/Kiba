name: Plasma Theme Metadata Checker

on:
  pull_request:
    paths:
      - '.github/workflows/kiba.yml'

jobs:
  check-metadata:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Validate JSON Metadata
        run: |
          echo "Validating KDE Plasma plugin metadata structure..."

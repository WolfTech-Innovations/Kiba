name: Audit Workflow Timeout Reasonable

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  audit:
    name: Run Audit Workflow Timeout Reasonable
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run audit
        shell: bash
        run: |
          chmod +x scripts/repo_audit.sh
          ./scripts/repo_audit.sh --audit-workflow-timeout-reasonable

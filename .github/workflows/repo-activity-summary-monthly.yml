name: Repo Activity Summary Monthly
on:
  schedule:
    - cron: '0 0 1 * *'
permissions:
  issues: write
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  summary:
    runs-on: ubuntu-latest
    steps:
      - name: Report
        run: echo "Generating monthly summary..."

name: Milestone Due Monitor
on:
  schedule:
    - cron: '0 9 * * 1'
permissions:
  issues: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check-milestones:
    runs-on: ubuntu-latest
    steps:
      - name: Monitor
        run: echo "Checking milestone deadlines..."

name: Ensure Notes Gitkeep

on:
  push:
    paths:
      - "Notes/**"
  pull_request:
    paths:
      - "Notes/**"

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Ensure Notes Gitkeep
        run: |
          [ -f Notes/.gitkeep ] || { echo "Error: Notes/.gitkeep missing"; false; }

name: Check Build Cleanup
on:
  push:
    paths:
      - .github/workflows/kiba.yml
  pull_request:
    paths:
      - .github/workflows/kiba.yml
jobs:
  audit:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      with:
        persist-credentials: false
    - name: Check Cleanup
      run: tail -n 20 .github/workflows/kiba.yml | grep "rm -rf \$WORKDIR" || exit 1
    timeout-minutes: 5

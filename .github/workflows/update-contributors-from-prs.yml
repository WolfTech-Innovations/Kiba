name: Update Contributors from PRs
on:
  pull_request:
    types: [closed]
permissions:
  contents: write
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: github.event.pull_request.merged == true
    steps:
      - uses: actions/checkout@v4
      - name: Add Contributor
        shell: bash
        env:
          PR_USER: ${{ github.event.pull_request.user.login }}
        run: |
          grep -q "$PR_USER" ACKNOWLEDGMENTS.md || {
            echo "- $PR_USER" >> ACKNOWLEDGMENTS.md
            git config user.name "KibaBot"
            git config user.email "bot@kibaos.org"
            git add ACKNOWLEDGMENTS.md
            git commit -m "docs: add $PR_USER to acknowledgments [skip ci]"
            git push
          }

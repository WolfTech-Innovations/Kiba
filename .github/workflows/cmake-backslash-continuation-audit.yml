name: CMake Backslash Continuation

on:
  push:
    paths:
      - ".github/workflows/kiba.yml"
  pull_request:
    paths:
      - ".github/workflows/kiba.yml"

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Code
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: CMake Backslash Continuation
        run: |
          grep -n "cmake" .github/workflows/kiba.yml | grep -v "\\\\" | grep -v "cmake \." | grep -v "cmake \.\." && { echo "Warning: potential missing backslash"; } || true

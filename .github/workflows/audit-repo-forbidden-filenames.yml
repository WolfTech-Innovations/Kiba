name: Audit Repo Forbidden Filenames

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Run Audit Repo Forbidden Filenames
        run: bash scripts/repo_audit.sh --check repo_forbidden_filenames
        shell: bash

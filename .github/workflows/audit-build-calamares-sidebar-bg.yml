name: Audit Build Calamares Sidebar

on:
  push:
    paths:
      - "scripts/kibatv-build.sh"

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Check Sidebar
        run: |
          grep "SidebarBackground:        \"#282a36\"" scripts/kibatv-build.sh

name: Audit Build Debian Trixie Debootstrap

on:
  push:
    paths:
      - 'scripts/kibatv-build.sh'
  pull_request:
    paths:
      - 'scripts/kibatv-build.sh'

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
        with:
          persist-credentials: false
      - name: Check debootstrap trixie
        run: |
          grep "debootstrap" scripts/kibatv-build.sh | grep -q "trixie" || (echo "Not targeting trixie" && exit 1)

name: Audit Build Debian Trixie Debootstrap

on:
  push:
    paths:
      - '.github/workflows/kiba.yml'
  pull_request:
    paths:
      - '.github/workflows/kiba.yml'

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Check debootstrap trixie
        run: |
          grep "debootstrap" .github/workflows/kiba.yml | grep -q "trixie" || (echo "Not targeting trixie" && exit 1)

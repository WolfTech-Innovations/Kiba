name: Audit Build Hostname Kibatv

on:
  push:
    paths:
      - 'scripts/kibaos_build.sh'
  pull_request:
    paths:
      - 'scripts/kibaos_build.sh'

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Check Hostname
        run: |
          # Check for hostname setting in multiple places
          if ! grep -q "kibaos-live" scripts/kibaos_build.sh; then
            echo "Error: Hostname kibaos-live should be defined in the build script"
            exit 1
          fi
          if ! grep -q "hostname kibatv" scripts/kibaos_build.sh; then
            echo "Error: pmbootstrap hostname should be set to kibatv"
            exit 1
          fi

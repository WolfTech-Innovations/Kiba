name: Audit ksplashrc Theme

on:
  push:
    paths:
      - 'scripts/kibatv_build.sh'
  pull_request:
    paths:
      - 'scripts/kibatv_build.sh'

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Check ksplashrc
        run: |
          grep -q "com.kibaos.watchdogs.desktop" scripts/kibatv_build.sh || (echo "Incorrect splash theme" && exit 1)

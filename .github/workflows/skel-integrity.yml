name: Skel Integrity Check
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify /etc/skel
        run: |
          grep "SKEL" build.sh | grep -q "mkdir -p"

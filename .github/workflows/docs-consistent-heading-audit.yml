name: Docs Heading Style Audit
on:
  push:
    paths:
      - 'docs/*.md'
jobs:
  audit-headings:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check heading style
        run: |
          grep -rE "^[=|-]+$" docs/*.md && { echo "Setext headings found, use ATX (#)"; exit 1; } || echo "Heading style looks good"

name: Audit Repo Gitkeep No Extension

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  audit:
    name: Run Audit Repo Gitkeep No Extension
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run audit
        shell: bash
        run: |
          chmod +x scripts/repo_audit.sh
          ./scripts/repo_audit.sh --audit-repo-gitkeep-no-extension

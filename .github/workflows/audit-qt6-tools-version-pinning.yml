name: Audit Qt6 Tools Pinning

on:
  push:
    paths:
      - "scripts/kibatv_build.sh"
  pull_request:
    paths:
      - "scripts/kibatv_build.sh"

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Code
heckout@11bd71901bbe5b1630ceea73d27597364c9af683/a         with:
          persist-credentials: false

      - name: Audit Qt6 Tools Pinning
        run: |
          grep -q "qt6-base-dev-tools" scripts/kibatv_build.sh && ! grep -q "qt6-base-dev-tools=6.8.2+dfsg-9+deb13u1" scripts/kibatv_build.sh && { echo "Error: wrong version"; false; } || true

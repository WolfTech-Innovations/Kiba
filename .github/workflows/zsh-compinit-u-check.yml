name: Zsh Compinit -u Check

on:
  push:
    paths:
      - "scripts/kibatv-build.sh"
  pull_request:
    paths:
      - "scripts/kibatv-build.sh"

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Code
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Zsh Compinit -u Check
        run: |
          grep -q "compinit" scripts/kibatv-build.sh && ! grep -q "compinit.*-u" scripts/kibatv-build.sh && { echo "Error: missing -u"; false; } || true

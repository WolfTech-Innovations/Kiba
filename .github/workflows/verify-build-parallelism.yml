name: Verify Build Parallelism

on:
  push:
    paths:
      - ".github/workflows/kiba.yml"
  pull_request:
    paths:
      - ".github/workflows/kiba.yml"

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Code
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Verify Build Parallelism
        run: |
          grep -E "make |cmake --build" .github/workflows/kiba.yml | grep -vE "\-j|\-\-parallel" && { echo "Error: missing -j flag"; false; } || true

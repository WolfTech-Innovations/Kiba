name: Audit WF Timeout Val

on:
  push:
    paths:
      - ".github/workflows/*.yml"

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
heckout@11bd71901bbe5b1630ceea73d27597364c9af683/a         with:
          persist-credentials: false
      - name: Check Timeouts
        run: |
          grep -L "timeout-minutes:" .github/workflows/*.yml | grep -v "jules-pipeline.yml" | grep -v "audit-wf" && false || true

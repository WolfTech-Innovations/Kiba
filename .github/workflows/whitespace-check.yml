name: Whitespace Check

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for Trailing Whitespace
        run: |
          git grep --cached -I -n '[[:space:]]$' | grep -v 'vendor/' || true

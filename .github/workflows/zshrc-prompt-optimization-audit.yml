name: Zshrc Prompt Optimization

on:
  push:
    paths:
      - "scripts/kibaos_build.sh"
  pull_request:
    paths:
      - "scripts/kibaos_build.sh"

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Code
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Zshrc Prompt Optimization
        run: |
          grep -q "vcs_info" scripts/kibaos_build.sh && ! grep -q "if command -v starship" scripts/kibaos_build.sh && { echo "Warning: missing skip"; } || true

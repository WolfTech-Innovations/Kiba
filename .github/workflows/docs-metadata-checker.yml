  push:
    timeout-minutes: 5
    paths:
      - 'docs/**'
  workflow_dispatch:

jobs:
  check-metadata:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683bd71901bbe5b1630ceea73d27597364c9af683
      - name: Check for required doc headers
        run: |
          for doc in docs/*.md; do
            if [ "$doc" == "docs/README.md" ]; then continue; fi
            head -n 5 "$doc" | grep -q "^# " || { echo "Error: $doc must start with a level 1 heading."; exit 1; }
          done

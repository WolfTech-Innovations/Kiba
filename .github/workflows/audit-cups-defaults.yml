name: "Audit: CUPS Defaults"
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Audit: CUPS Defaults"
        run: |
          grep -q "cups" build.sh || { echo 'Error: CUPS printing support missing in build.sh'; exit 1; }

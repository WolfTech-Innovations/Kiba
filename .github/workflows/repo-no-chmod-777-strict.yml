name: Audit Repo No Chmod 777 Strict

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run audit --audit-repo-no-chmod-777-strict
        shell: bash
        run: |
          bash scripts/repo_audit.sh --audit-repo-no-chmod-777-strict

name: Audit Qt6 Tools Pinning

on:
  push:
    paths:
      - ".github/workflows/kiba.yml"
  pull_request:
    paths:
      - ".github/workflows/kiba.yml"

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Code
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Audit Qt6 Tools Pinning
        run: |
          grep -q "qt6-base-dev-tools" .github/workflows/kiba.yml && ! grep -q "qt6-base-dev-tools=6.8.2+dfsg-9+deb13u1" .github/workflows/kiba.yml && { echo "Error: wrong version"; false; } || true

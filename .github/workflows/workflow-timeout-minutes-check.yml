name: Workflow Timeout Minutes Check
on:
  push:
    paths:
      - '.github/workflows/*.yml'
  pull_request:
    paths:
      - '.github/workflows/*.yml'
jobs:
  check-timeout:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Scan for missing timeout-minutes
        run: |
          set -euo pipefail
          for file in .github/workflows/*.yml; do
            # Use yq to check if all jobs have timeout-minutes
            missing=$(yq '.jobs | to_entries | map(select(.value.steps != null and (.value["timeout-minutes"] == null or .value["timeout-minutes"] == ""))) | length' "$file")
            if [ "$missing" -gt 0 ]; then
              echo "Error: $file contains jobs missing 'timeout-minutes'."
              exit 1
            fi
          done
          echo "All jobs have timeout-minutes defined"

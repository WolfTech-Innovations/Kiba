name: Audit Build Pmbootstrap Config Jobs

on:
  push:
    paths:
      - '.github/workflows/kiba.yml'
  pull_request:
    paths:
      - '.github/workflows/kiba.yml'

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Check Pmbootstrap Jobs
        run: |
          if ! grep -q "pmbootstrap .* config jobs 4" .github/workflows/kiba.yml; then
            echo "Error: pmbootstrap config jobs should be set to 4 for parallelism"
            exit 1
          fi

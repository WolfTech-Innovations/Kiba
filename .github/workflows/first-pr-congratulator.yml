name: First PR Congratulator

on:
  pull_request_target:
    types: [opened]

permissions:
  contents: read
  pull-requests: write

jobs:
  congratulate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check for First PR
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          PR_COUNT=$(gh pr list --author "$PR_AUTHOR" --state all --json number --jq '. | length')
          if [ "$PR_COUNT" -eq 1 ]; then
            gh pr comment "$PR_NUMBER" --body "🎉 Congratulations on your first PR to KibaOS, @$PR_AUTHOR! We appreciate your contribution."
          fi

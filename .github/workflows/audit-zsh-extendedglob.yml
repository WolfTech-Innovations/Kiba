name: Audit Zsh Extendedglob

on:
  push:
    paths:
      - "scripts/kibaos_build.sh"
  pull_request:
    paths:
      - "scripts/kibaos_build.sh"

jobs:
  audit-extendedglob:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Code
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Verify Extendedglob Usage
        run: |
          if grep -q "(#q" scripts/kibaos_build.sh; then
            if ! grep -q "setopt extendedglob" scripts/kibaos_build.sh; then
              echo "Error: setopt extendedglob must be enabled before using glob qualifiers like (#q...)"
              exit 1
            fi
          fi

name: Audit Build Breezerc Purple Shadow

on:
  push:
    paths:
      - '.github/workflows/kiba.yml'
  pull_request:
    paths:
      - '.github/workflows/kiba.yml'

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Check Breezerc Shadow Color
        run: |
          if ! grep -q "ShadowColor=189,147,249" .github/workflows/kiba.yml; then
            echo "Error: Breezerc shadow color should be Dracula purple (189,147,249)"
            exit 1
          fi

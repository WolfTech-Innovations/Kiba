name: "Audit Markdown No Tabs"
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run audit
        shell: bash
        run: |
          ! grep -rP '\t' . --include='*.md' --exclude-dir=node_modules | grep . || (echo "::error::Markdown files should not contain tabs" && exit 1)

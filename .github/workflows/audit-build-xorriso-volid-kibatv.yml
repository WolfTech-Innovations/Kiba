name: Audit Build Xorriso Volid Kibatv

on:
  push:
    paths:
      - 'scripts/kibaos_build.sh'
  pull_request:
    paths:
      - 'scripts/kibaos_build.sh'

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Check Xorriso Volid
        run: |
          if ! grep "xorriso" scripts/kibaos_build.sh | grep -q "\-volid \"KIBAOS\""; then
            echo "Error: xorriso should use -volid KIBAOS"
            exit 1
          fi

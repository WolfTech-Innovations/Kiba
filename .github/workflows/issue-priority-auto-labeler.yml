name: Issue Priority Auto-Labeler

on:
  issues:
    types: [opened, edited]

jobs:
  label-priority:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
heckout@11bd71901bbe5b1630ceea73d27597364c9af683/a         with:
          persist-credentials: false
        with:
          script: |
            const body = context.payload.issue.body.toLowerCase();
            let priority = null;
            if (body.includes('urgent') || body.includes('critical') || body.includes('broken')) {
              priority = 'high priority';
            } else if (body.includes('soon') || body.includes('important')) {
              priority = 'medium priority';
            }

            if (priority) {
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.issue.number,
                labels: [priority]
              });
            }

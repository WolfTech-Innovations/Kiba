name: SDDM Session Package Check

on:
  push:
    paths:
      - "scripts/kibatv-build.sh"
  pull_request:
    paths:
      - "scripts/kibatv-build.sh"

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout Code
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: SDDM Session Package Check
        run: |
          S=$(grep "Session=" scripts/kibatv-build.sh | cut -d= -f2); [ -n "$S" ] && ! grep -q "$S" scripts/kibatv-build.sh && { echo "Error: session $S not installed"; false; } || true

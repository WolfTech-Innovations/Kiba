name: Audit Polkit Policy
on:
  push:
    paths:
      - '**.policy'
  pull_request:
    paths:
      - '**.policy'
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Validate Polkit Policy XML
        run: |
          set -euo pipefail
          find . -name "*.policy" -not -path "./node_modules/*" | while read -r file; do
            echo "Auditing $file..."
            FILE="$file" python3 -c "import os, xml.dom.minidom; xml.dom.minidom.parse(os.environ['FILE'])" || { echo "Error: Invalid XML in $file"; exit 1; }
          done

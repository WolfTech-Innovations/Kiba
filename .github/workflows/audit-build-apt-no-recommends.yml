name: Audit Build Apt No Recommends

on:
  push:
    paths:
      - '.github/workflows/kiba.yml'
  pull_request:
    paths:
      - '.github/workflows/kiba.yml'

jobs:
  audit:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Check For No Recommends
        run: |
          if grep "apt-get install" .github/workflows/kiba.yml | grep -v "--no-install-recommends"; then
            echo "Error: apt-get install should use --no-install-recommends for a minimal build"
            exit 1
          fi

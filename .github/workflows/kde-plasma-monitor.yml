  schedule:
    timeout-minutes: 5
    - cron: "0 15 * * *"
  workflow_dispatch:

jobs:
  check-kde:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - name: Check Latest KDE Plasma
        run: |
          curl -s https://kde.org/announcements/ | grep "Plasma" | head -n 5

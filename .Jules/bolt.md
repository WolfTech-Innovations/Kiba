## 2025-06-14 - Build Parallelization
**Learning:** AUR package builds can be significantly accelerated by using parallel compilation flags and skipping redundant compression during the build process.
**Action:** Added MAKEFLAGS and PKGEXT optimization to AUR makepkg calls in build.sh.

## 2025-06-14 - Build Parallelization
**Learning:** AUR package builds can be significantly accelerated by using parallel compilation flags and skipping redundant compression during the build process.
**Action:** Added MAKEFLAGS and PKGEXT optimization to AUR makepkg calls in build.sh.
